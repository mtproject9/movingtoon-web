# 무빙툰 스튜디오 작업 시작 가이드

컴퓨터를 껐다 켠 뒤, 이 프로젝트로 작업을 시작할 때 매번 따라 하면 되는 순서입니다.

## 0. 미리 알아둘 것 (한 번만 읽으면 됨)

- **프로젝트 폴더 위치**: `C:\Users\Gong\Desktop\무빙툰 스튜디오`
  이 폴더가 우리 프로젝트의 전부입니다. 이 폴더를 지우거나 이름을 바꾸면 안 됩니다.
- **터미널이란?**: 마우스 클릭 대신 글자로 명령을 입력해서 컴퓨터에게 일을 시키는 검은 화면입니다.
- **로컬 서버란?**: 인터넷에 올리기 전에, 내 컴퓨터 안에서만 "무빙툰 스튜디오" 앱을 미리 실행해보는 것입니다. 서버를 켜야 `localhost:3000` 주소로 앱을 볼 수 있습니다.
- **localhost:3000**: "내 컴퓨터 안에서 실행 중인 사이트"를 보는 주소입니다. 인터넷에 있는 주소가 아니라, 내 컴퓨터에서만 보이는 주소입니다.

---

## 데이터는 이제 어디에 저장되나요? (2026-08-31 업데이트)

예전에는 시리즈·회차·컷·이미지 같은 데이터가 **브라우저 안**에만 저장돼서, 브라우저가
캐시를 지우거나 하면 통째로 사라지는 문제가 있었습니다.

이제는 이런 데이터가 **진짜 데이터베이스(Postgres)** 와 **파일 저장소(Vercel Blob)** 에
저장됩니다. 은행 계좌 정보처럼 별도로 안전하게 보관되는 곳이라, 브라우저를 지우든
컴퓨터를 바꾸든 데이터가 사라지지 않습니다.

- **바뀐 것**: 시리즈, 회차, 캐릭터, 컷(대사/원고), 컷 이미지·음성 파일 → 전부 서버에 안전하게 저장됨
- **아직 브라우저에 저장되는 것**: 캐릭터 참조 이미지 갤러리(캐릭터 사진 여러 장 모아두는 기능)는 아직 예전 방식이라, 이 부분만 브라우저 데이터가 지워지면 함께 사라질 수 있습니다.
- 이 변경 이후로는 그래도 **"데이터 백업" 버튼을 가끔 눌러 파일로 저장해두는 습관**을 유지하시는 걸 추천드려요 (특히 캐릭터 참조 이미지 갤러리 때문에요).

---

## 1단계. 프로젝트 폴더로 이동해서 터미널 열기

1. 바탕화면에서 **`무빙툰 스튜디오`** 폴더를 더블클릭해서 엽니다.
2. 폴더 안의 빈 곳(파일이 없는 공간)에서 **마우스 오른쪽 버튼 클릭**
3. 메뉴에서 **"터미널에서 열기"** 클릭
   (이 메뉴가 안 보이면: 시작 메뉴에서 "PowerShell" 검색 → 실행 → 아래 명령어로 폴더 이동)
   ```
   cd "C:\Users\Gong\Desktop\무빙툰 스튜디오"
   ```

## 2단계. 로컬 서버 켜기

터미널에 아래 명령어를 입력하고 Enter를 누릅니다.

```
npm run dev
```

몇 초 기다리면 아래처럼 초록색 글씨로 "Ready" 메시지가 뜹니다. 이게 뜨면 서버가 켜진 것입니다.

```
✓ Ready in 985ms
- Local: http://localhost:3000
```

**주의**: 이 터미널 창을 닫으면 서버도 꺼집니다. 작업하는 동안은 이 창을 계속 열어두세요.

## 3단계. 브라우저에서 앱 확인하기

크롬(또는 아무 브라우저)을 열고 주소창에 아래를 입력합니다.

```
localhost:3000
```

"무빙툰 스튜디오" 화면이 보이면 정상입니다. 여기서 시리즈를 만들고 실제로 앱을 써볼 수 있습니다.

## 4단계. Claude Code로 코드 수정 요청하기

1. Claude Code 앱을 실행합니다.
2. 왼쪽 사이드바에서 **`movingtoon-web`** 항목을 찾습니다 (이 폴더에 연결된 프로젝트입니다).
3. 새로 대화를 시작하려면 `movingtoon-web` 옆의 **"+"** 버튼 클릭 → 자동으로 이 프로젝트 폴더를 대상으로 새 채팅이 열립니다.
   이전 대화를 이어가려면, 그 아래 있는 기존 채팅 목록에서 원하는 대화를 클릭하면 됩니다.
4. 하고 싶은 작업을 말로 설명하면 됩니다. (예: "새 시리즈 만들기 버튼 색을 파란색으로 바꿔줘")
5. 수정이 끝나면, 2단계에서 켜둔 `localhost:3000` 화면을 새로고침해서 바로 확인할 수 있습니다.

## 5단계. 작업을 마칠 때

- 터미널 창을 그냥 닫으면 됩니다. (서버가 꺼집니다)
- 다음에 다시 시작할 때는 1단계부터 반복하면 됩니다.

---

## 다른 사람에게 보여줄 수 있게 실제 배포하기

로컬(`localhost:3000`)에서 확인한 내용을, 다른 사람도 볼 수 있는 실제 링크
(`https://movingtoon-web.vercel.app`)에 반영하고 싶을 때는 별도 문서를 참고하세요.

→ **[DEPLOY.md](./DEPLOY.md)** 파일 참고 (같은 폴더 안에 있음)

핵심만 요약하면:
```
npx vercel --prod
```
이 명령어 한 줄만 실행하면 됩니다.

---

## 주의사항 (중요)

- **`무빙툰 스튜디오` 폴더를 삭제/이동하지 마세요.** 실수로 지워졌던 적이 있습니다.
  휴지통에 폴더가 보이면 절대 "휴지통 비우기"를 누르지 말고, 먼저 확인해주세요.
- 이 폴더 안의 `node_modules`, `.next` 폴더는 용량이 커도 삭제하면 안 됩니다 (앱 실행에 필요한 부품들).
- `.env.local` 파일에는 비밀 키가 들어있을 수 있어서, 이 파일 내용을 다른 사람이나 채팅에 그대로 붙여넣지 마세요.

---

## 문제가 생겼을 때

- `localhost:3000`이 안 열린다 → 2단계(`npm run dev`)를 다시 실행했는지 확인
- 터미널에 빨간 글씨(Error)가 뜬다 → Claude Code에게 그 에러 메시지를 그대로 보여주고 물어보기
- 실제 사이트(`movingtoon-web.vercel.app`)에 최신 수정사항이 안 보인다 → `npx vercel --prod`를 다시 실행했는지 확인
