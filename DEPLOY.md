# 배포(재배포) 방법

이 문서는 코드를 수정한 뒤, 실제 사이트(https://movingtoon-web.vercel.app)에
그 수정사항을 반영하는 방법을 정리한 것입니다.

## 핵심 한 줄 요약

> 코드 수정 → `npx vercel --prod` 실행 → 몇 분 뒤 사이트에 반영

코드를 아무리 고쳐도, 이 명령어를 실행하기 전까지는 실제 사이트(다른 사람이 보는 링크)는
바뀌지 않습니다. 마치 유튜브 원본 파일을 수정해도 "업로드"를 눌러야 실제 영상이
바뀌는 것과 같습니다.

## 실행 방법

1. 탐색기에서 아래 폴더로 이동
   ```
   C:\Users\Gong\Desktop\무빙툰 스튜디오
   ```
2. 폴더 안 빈 곳에서 마우스 오른쪽 버튼 클릭 → **"터미널에서 열기"** (또는 PowerShell 실행 후 `cd` 로 이 폴더까지 이동)
3. 아래 명령어를 입력하고 Enter

   ```
   npx vercel --prod
   ```

4. "Ready in ...s" 메시지와 함께 아래 주소가 다시 표시되면 완료된 것입니다.

   ```
   https://movingtoon-web.vercel.app
   ```

## 명령어 뜻풀이

| 부분 | 의미 |
|---|---|
| `npx` | Vercel 프로그램을 따로 설치하지 않고 그때그때 불러와서 실행 |
| `vercel` | Vercel 서비스에게 "이 폴더의 코드를 업로드/배포해줘"라고 요청 |
| `--prod` | 임시 테스트 링크가 아니라, 실제 정식 주소(production)로 배포 |

`--prod` 를 빼고 그냥 `npx vercel` 만 실행하면, 실제 링크가 아니라
확인용 임시 링크만 새로 생기니 주의하세요.

## 참고

- GitHub과는 연동하지 않았으므로, GitHub에 코드를 올리는 것만으로는
  사이트가 자동으로 바뀌지 않습니다. 반드시 위 명령어를 직접 실행해야 합니다.
- Gemini API 키 등 민감한 값은 Vercel에 환경변수로 이미 등록되어 있으므로,
  재배포 시 따로 다시 입력할 필요가 없습니다.
